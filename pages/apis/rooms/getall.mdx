import Badge from "@components/built-in/Badge";

## Get All Rooms `MVP`

**Request :**

- Method : <Badge method="GET">GET</Badge>
- Endpoint : `/api/rooms`
- Headers :
  - `Content-Type: application/json`
  - `Accept: application/json`

**Response :** <Badge outline variant="success">200 OK</Badge>

```json
{
  "code": "number",
  "status": "string, enum",
  "data": [
    {
      "id": "string, unique",
      "title": "string",
      "description": "string",
      "type": "string, enum",
      "payment_period": "string, enum",
      "images": ["string"],
      "location": "string",
      "facilities": ["string"]
    }
  ]
}
```
