import Badge from "@components/built-in/Badge";

## Get All Transaction `MVP`

**Request :**

- Method : <Badge method="GET">GET</Badge>
- URL : `https://be-java-binar-fp-staging.up.railway.app`
- Endpoint : `/v1/transactions/history` or else
- Headers :
  - `Authorization: Bearer <access_token>`
  - `Content-Type: application/json`
  - `Accept: application/json`

**Response :** <Badge outline variant="success">200 OK</Badge>

```json
{
  "status": "string",
  "message": "string",
  "data": [
    {
      "id": "number",
      "name": "string",
      "status": "string, onproccess|canceled|approved|rejected|ongoing|ended",
      "label": "string",
      "thumbnail": "string",
      "address": "string",
      "type": "string",
      "rating": "number"
    }
  ]
}
```
