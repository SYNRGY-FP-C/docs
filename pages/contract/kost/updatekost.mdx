import Badge from "@components/built-in/Badge";

## Update Kost `MVP`

**Request :**

- Method : <Badge method="PUT">PUT</Badge>
- URL : `https://be-java-binar-fp-staging.up.railway.app`
- Endpoint : `/v1/kost/updatekost` or else
- Headers :
  - `Content-Type: multipart/form-data`

**Body :**

```json
{
  "name": "string",
  "image_1": "file, png|jpg|jpeg",
  "image_2": "file, png|jpg|jpeg",
  "type": "string, enum",
  "description": "string",
  "location": {
    "long": "string",
    "lat": "string",
    "address": "string",
    "province": "string",
    "city": "string",
    "district": "string",
    "note": "string"
  },
  "payment_scheme": [
    {
      "id": "number"
    }
  ],
  "rules": [
    {
      "id": "number"
    }
  ],
  "additional_rule": "string",
  "facilities": [
    {
      "id": "number"
    }
  ]
}
```

**Response :** <Badge outline variant="success">200 OK</Badge>

```json
{
  "status": "string",
  "message": "string"
}
```
