import Badge from "@components/built-in/Badge";

## Room Submission `MVP`

**Request :**

- Method : <Badge method="POST">POST</Badge>
- URL : `https://be-java-binar-fp-staging.up.railway.app`
- Endpoint : `/v1/transactions/booking` or else
- Headers :
  - `Authorization: Bearer <accessToken>`
  - `Content-Type: application/json`
  - `Accept: application/json`

**Body :**

```json
{
  "kost_id": "string",
  "user_id": "string",
  "capacity": "number",
  "start_date": "date",
  "payment_scheme": "string",
  "addons_facilities": [{ "id": "number" }]
}
```

**Response :** <Badge outline variant="success">201 Created</Badge>

```json
{
  "status": "string",
  "message": "string"
}
```
