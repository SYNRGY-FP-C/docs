## Contribute

### 1. Clone repository

```bash
git clone -b <branchname> <remote-repo-url>
```

```bash filename="Example Using SSH"
git clone -b development git@github.com:SYNRGY-FP-C/frontend-final-project.git
git clone -b development git@github.com:SYNRGY-FP-C/backend-final-project.git
```

```bash filename="Example Using HTTPS"
git clone -b development https://github.com/SYNRGY-FP-C/frontend-final-project.git
git clone -b development https://github.com/SYNRGY-FP-C/backend-final-project.git
```

### 2. Checkout branch feature/namafitur-step

```bash
git checkout -b feature/namafitur-step
```

```bash filename="Example"
git checkout -b feature/login-slicing
```

### 3. Setup .env

See [Environtment Variables](https://github.com/SYNRGY-FP-C/dotenv)

Copy and paste the value from `.env` file.

### 4. Ngoding

### 5. Pull dari branch development

```bash
git pull origin <parrent-branch>
```

```bash filename="Example"
git pull origin development
```

### 6. Add, Commit dan Push ke branch feature/namafitur-step

```bash
git add .
```

```bash
git commit -m "commit message"
```

```bash
git push origin feature/namafitur-step
```

```bash filename="Example"
git add .
git commit -m "slicing login page"
git push origin feature/login-slicing
```

### 7. Pull Request ke branch development di github

### 8. Tunggu review dari team

Kembali ke branch `development` ketika menambah fitur baru dan lakukan step 2-7
